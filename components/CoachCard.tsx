import Link from 'next/link';
import { MapPin, Star, Clock3, ArrowUpRight } from 'lucide-react';
export function CoachCard({coach}:{coach:any}){return <Link href={`/coaches/${coach.id}`} className="coachCard"><div className={`avatar avatar-${coach.accent}`}><span>{coach.initials}</span><div className="online"/></div><div className="coachTop"><span className="pill muted">{coach.tag}</span><span className="rating"><Star size={14} fill="currentColor"/> {coach.rating} <em>({coach.reviews})</em></span></div><h3>{coach.name}</h3><p className="role">{coach.role}</p><div className="meta"><span><MapPin size={15}/>{coach.area}</span><span><Clock3 size={15}/>{coach.availability}</span></div><div className="specialties">{coach.specialties.map((s:string)=><span key={s}>{s}</span>)}</div><div className="cardBottom"><strong>SAR {coach.price}<small>/session</small></strong><span className="roundArrow"><ArrowUpRight size={18}/></span></div></Link>}
