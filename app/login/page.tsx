'use client';
import Link from 'next/link';import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';import {Nav} from '@/components/Nav';import {ArrowRight,Check,UserRound,Dumbbell} from 'lucide-react';import {useLanguage} from '@/components/LanguageProvider';import {api} from '@/lib/api';
export default function Login(){const{lang}=useLanguage();const ar=lang==='ar';const router=useRouter();const[role,setRole]=useState('client');const[form,setForm]=useState({name:'',email:'',phone:'',password:''});const[error,setError]=useState('');const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await api<any>('/auth/register',{method:'POST',body:JSON.stringify({...form,role})});localStorage.setItem('pc_token',r.access_token);localStorage.setItem('pc_user',JSON.stringify(r.user));router.push(role==='coach'?'/coach-portal':'/start')}catch(x:any){setError(x.message)}finally{setBusy(false)}}return <><Nav/><main className="authPage"><section className="authPitch"><span className="kicker">PRIVATE COACH</span><h1>{ar?'ابدأ رحلتك، بطريقتك.':'Start your journey, your way.'}</h1><p>{ar?'احجز مدربًا يأتي إلى منزلك، تدرب أونلاين، أو اجمع الاثنين مع مدرب AI.':'Book a coach at home, train online, or combine both with AI.'}</p>{['مدربون موثّقون','دفع آمن','خطة وتقدم في مكان واحد'].map(x=><div className="authPoint" key={x}><Check/>{x}</div>)}</section><form className="authCard" onSubmit={submit}><div className="roleSwitch"><button type="button" className={role==='client'?'active':''} onClick={()=>setRole('client')}><UserRound/>{ar?'متدرب':'Client'}</button><button type="button" className={role==='coach'?'active':''} onClick={()=>setRole('coach')}><Dumbbell/>{ar?'مدرب':'Coach'}</button></div><h2>{ar?'إنشاء حساب جديد':'Create your account'}</h2><label>{ar?'الاسم الكامل':'Full name'}<input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>{ar?'رقم الجوال':'Mobile number'}<div className="phoneInput"><span>+966</span><input required value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></div></label><label>{ar?'البريد الإلكتروني':'Email'}<input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>{ar?'كلمة المرور':'Password'}<input required minLength={8} type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{error&&<p className="formError">{error}</p>}<button disabled={busy} className="primary authSubmit">{busy?(ar?'جاري التسجيل...':'Creating...'):(ar?'إنشاء الحساب':'Create account')}<ArrowRight/></button><p className="terms">{ar?'بالمتابعة أنت توافق على الشروط وسياسة الخصوصية.':'By continuing you agree to our terms and privacy policy.'}</p>{role==='coach'&&<Link className="coachHint" href="/coach-portal">{ar?'بعد التسجيل: أكمل التوثيق وأنشئ ملفك كمدرب':'Next: verify and create your coach profile'}</Link>}</form></main></>}
